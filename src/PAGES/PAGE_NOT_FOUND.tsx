import React, { FC } from 'react';

const PNF_PAGE_JSX: FC = (): JSX.Element => {
    return (
        <React.StrictMode>
            <div className="PNF_WRAPPER">
                <h1>PAGE NOT FOUND - ERRCODE (404)</h1>
            </div>
        </React.StrictMode>
    );
};

export default PNF_PAGE_JSX;