import React, { FC } from 'react';

const SIGNUP_PAGE_JSX: FC = (): JSX.Element => {
    return (
        <React.StrictMode>
            <h1>SIGNUP</h1>
        </React.StrictMode>
    );
};

export default SIGNUP_PAGE_JSX;