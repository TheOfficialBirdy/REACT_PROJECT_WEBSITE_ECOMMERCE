import React, { FC } from 'react';

const HEROBANNER_COMPONENT_JSX: FC = (): JSX.Element => {
    return (
        <React.Fragment>
            <section>
                
            </section>
        </React.Fragment>
    );
};