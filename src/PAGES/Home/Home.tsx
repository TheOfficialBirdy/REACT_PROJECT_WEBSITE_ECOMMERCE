import React, { FC } from 'react';

const HOME_PAGE_JSX: FC = (): JSX.Element => {
    return (
        <React.StrictMode>
            <h1>HOME</h1>
        </React.StrictMode>
    )
};

export default HOME_PAGE_JSX;